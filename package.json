{
  "name": "playwright",
  "description": "Playwright tests with Allure reporting and GitHub Actions with integration",
  "scripts": {
    "test:all": "sh -c 'for f in $(ls conf/*.config.js | grep -v \"common.config.js\"); do echo Running $f; npx playwright test --config=$f; done'",
    "allure:generate": "npx allure-commandline generate allure-results --clean -o allure-report",
    "allure:open": "npx allure-commandline open allure-report",
    "allure:serve": "npx allure-commandline serve allure-results"
  },
  "dependencies": {
    "allure-playwright": "3.0.6",
    "dotenv": "^10.0.0",
    "googleapis": "^173.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "allure-commandline": "^2.34.1",
    "playwright": "1.41.0"
  },
  "allure": {
    "outputDir": "allure-results"
  }
}
